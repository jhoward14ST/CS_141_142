
public class P4extras {
	// HERE LIES EXTRA CODE NOT CURRENTLY NEEDED
//	String prompt = "Please enter the number of pieces for each player (1-10):";
//	String invalidPrompt = "That was not a valid number! Please try again. ";
//	String illegalPrompt = "That space does not contain a piece you can move! Please try again. ";
//	String p1Prompt = "Crickets, please enter your move (1-";
//	String p2Prompt = "Grasshoppers, please enter your move (1-";
	//int[] testArray = createBoard(boardInputs[0], boardInputs[1]);
	// Below is first working method for createBoard
	//public static int[] createBoard(int piecesPerPlayer, int spacesInMiddle) {
//		int boardSize = ((piecesPerPlayer * 2) + spacesInMiddle);
//		int[] boardArray = new int [boardSize];
//		return boardArray;
//	 	System.out.println(boardToString(createBoard(boardInputs[0], boardInputs[1])));

	//int[] converterArray = (createBoard(boardInputs[0], boardInputs[1]));
	//
	//public static String boardToString(int[] board) {
//		int[] converterArray = (createBoard(boardInputs[0], boardInputs[1]));
//		String newBoard = converterArray.toString();
//		newBoard = String.join("");
//		return newBoard;
	//public static String boardToString(int[] array) {
//		String newBoard = array.toString().replace(",", "").replace("[", "").replace("]", "").trim();
//		return newBoard;
	//System.out.println(boardToString(createBoard(boardInputs[0], boardInputs[1])));
	//printArray(boardArray);
	//System.out.print("Please enter the number of pieces for each player
	//(1-10):");
	//System.out.print("That was not a valid number! Please try again. ");
	//System.out.print("Please enter the number of spaces in the middle (1-9): ");
	//public static void moveTester(int[] arr, int piece, int openSpace) {
//		int temp = arr[piece]; // saves the old value
//		arr[piece] = arr[openSpace]; // overwrites arr[i]
//		arr[openSpace] = temp; // gives arr[j] the old value for arr[i]
	//if ((board[i + 1] == 0) && (board[i + 2] == 2)) {
	//loserCountP1++;
	//}
	//if ((board[i + 1] == 2) && (board[i + 2] == 2)) {
	//loserCountP1++;
	//}
	//if ((board[i + 1] != 1) && (board[i + 1] == board[boardSize - 1])) {
	//loserCountP1++;
	//String invalidPrompt, String illegalPrompt) {
	//int[] playingBoardArray = (createBoard(boardInputs[0], boardInputs[1]));
	//String boardWithNumbers = (boardToString(playingBoardArray));
	//String boardWithLetters = numToLetters(boardWithNumbers);
	//int player = 1;
	//while (true) {
	//
	//// while (player == 1) {
	//if (!canMove(playingBoardArray, 2)) {
//		System.out.println(p2Won);
//		return 0;
	//}
	//if (canMove(playingBoardArray, 1)) {
//		System.out.print(p1Prompt + playingBoardArray.length + ") :");
//		if (!s.hasNextInt()) {
//			System.out.println(invalidPrompt);
//			s.nextLine();
//		}
//		if (s.hasNextInt()) {
//			int player1Move = s.nextInt() - 1;
//			if (!isMoveValid(playingBoardArray, player1Move, 1)) {
//				System.out.println(illegalPrompt);
//				s.nextLine();
//			}
//			if (!isMoveLegal(playingBoardArray, player1Move, 1)) {
//				System.out.println(illegalPrompt);
//				s.nextLine();
//			} else {
//				move(playingBoardArray, 1, player1Move);
//				boardWithNumbers = (boardToString(playingBoardArray));
//				boardWithLetters = numToLetters(boardWithNumbers);
//				//printArray(losingCount);
//				System.out.println(canMove(playingBoardArray, 1));
//				print(boardWithLetters);
//				s.nextLine();
//			}
//		}
	//}
	//// while (player == 2) {
	//if (!canMove(playingBoardArray, 1)) {
//		System.out.println(p1Won);
//		return 0;
	//}
	//if (canMove(playingBoardArray, 2)) {
//		System.out.print(p2Prompt + playingBoardArray.length + ") :");
//		if (!s.hasNextInt()) {
//			System.out.println(invalidPrompt);
//			s.nextLine();
//		}
//		if (s.hasNextInt()) {
//			int player2Move = s.nextInt() - 1;
//			if (!isMoveValid(playingBoardArray, player2Move, 2)) {
//				System.out.println(illegalPrompt);
//				s.nextLine();
//			}
//			if (!isMoveLegal(playingBoardArray, player2Move, 2)) {
//				System.out.println(illegalPrompt);
//				s.nextLine();
//			} else
//				move(playingBoardArray, 2, player2Move);
//			boardWithNumbers = (boardToString(playingBoardArray));
//			boardWithLetters = numToLetters(boardWithNumbers);
//			printArray(losingCount);
//			System.out.println("");
//			System.out.println(canMove(playingBoardArray, 2));
//			print(boardWithLetters);
//			s.nextLine();
//			player = 1;
	//public static void indexFinder(int arr[], int number, int player) {
	//arr = (playingBoardArray);
	//int newArray = [];
	//for (int i = 0; i < arr.length; i++) {
//		if (arr[i].indexOf(player) >= 0)
//			newArray.push(i);
	//public static String editor(String oldString) {
//		oldString = (oldString.replace('0', 'C'));
//		oldString = (oldString.replace('1', '.'));
//		oldString = (oldString.replace('2', 'G'));
//		oldString = (oldString.replace(",", "").replace("[", "").replace("]", "").trim());
//		oldString = (oldString.replace(" ", ""));
//		return oldString;
	//}
//				j++;
//				return i;
//			} else {
//				i = i + 1;
//			}
//		}
//		return -1;
//		if (board[position] == 0) {
//		return true;
	//if (board[position] == 2) {
//		return true;
	//}
	//}

	//}
	//}
	//}
	//}
	//}
	//}
	//}
}
